#this have to be on master branch
#this is global config, available for all profiles and all environments

eureka:
  client:
    enabled: true

bt:
  wallet-service:
    url: https://bluetrade-server-bt-wallet-service
  currency-receiver:
    #    url: https://bluetrade-server-bt-currency-receiver
    url: localhost:8083
  currency-order:
    url: https://bluetrade-server-bt-currency-order